# Changelog

## 1.0.1 (2026-09-25)

- README in English and Russian, own icon.
- Proprietary license for the patch scripts; ZAutoBroadcast stays with its author.
- Release workflow: compiles the patch class and publishes the patch kit without the plugin itself.

## 1.0.0 (2026-09-24)

- Adventure libraries in `plugin.yml`, ASM patch of the three `sendMessage(Component)` calls, interface flag fix for `legacySection()`.
